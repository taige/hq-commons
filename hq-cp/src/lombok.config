# 1. 告诉 Lombok 可以在这里停止向父目录查找配置 (可选，但在根目录推荐加上)
config.stopBubbling = true

# 2. 核心配置：让 Lombok 给生成的方法添加 @lombok.Generated 注解
lombok.addLombokGeneratedAnnotation = true

lombok.log.fieldname=LOGGER
lombok.log.custom.declaration=io.hqwu.commons.util.Logger io.hqwu.commons.util.LoggerFactory.getLogger()